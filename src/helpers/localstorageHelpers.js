export const LOCAL_STORAGE_KEYS = {
    canvasState: 'CANVAS_STATE',
}

export const saveCanvasStateToLocalStorage = (stateObj) => {
    // let canvasState = localStorage.getItem(LOCAL_STORAGE_KEYS.canvasState) || []
    // canvasState = canvasState.push(stateObj);
    // localStorage.setItem(LOCAL_STORAGE_KEYS.canvasState, JSON.stringify(canvasState))
    console.log("LOCAL");

}